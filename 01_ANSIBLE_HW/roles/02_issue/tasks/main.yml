---
- name: define content of /etc/issue as variable
  shell: cat /etc/issue
  register: etc_issue

- name: define hostname as variable  
  shell: hostname
  register: hostname

- name: printing hostnames together with registered variables will be a plus.
  debug: 
    msg: "HOSTNAME: {{ hostname.stdout }} ISSUE: {{ etc_issue.stdout }}" 